# Smart Office Waste Management System — Python dependencies
# Install with: pip install -r requirements.txt
#
# Note: RPi.GPIO, sense-hat, and picamera2 are pre-installed on Raspberry Pi OS.
# adafruit-circuitpython-dht also requires libgpiod2:
#   sudo apt install libgpiod2

RPi.GPIO>=0.7.1
adafruit-circuitpython-dht>=3.7.0
sense-hat>=2.3.0
RPLCD>=1.3.0
picamera2>=0.3.12
paho-mqtt>=2.0.0
flask>=3.0.0
Pillow>=10.0.0
requests>=2.31.0
